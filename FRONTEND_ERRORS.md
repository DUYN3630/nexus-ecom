| 1 | ReferenceError: Wrench is not defined | Quên import icon `Wrench` từ thư viện `lucide-react`. | Import `Wrench` trong `AdminSidebar.jsx` hoặc file liên quan. | ✅ Thành công |
| 2 | AxiosError 403 Forbidden | User đăng nhập có role `Customer` trong DB nên bị Backend chặn quyền Admin. | Chạy script Backend cập nhật role user thành `admin` và yêu cầu user Re-login. | ✅ Thành công |
| 3 | TypeError: productApi.getProducts is not a function | Gọi sai tên hàm trong `productApi.js` (hàm chuẩn là `getAll`). | Đổi `productApi.getProducts` thành `productApi.getAll` trong `ComparePage.jsx`. | ✅ Thành công |
| 4 | Lỗi cấu trúc JSX khi Refactor Admin Pages | Sử dụng `replace` với placeholder `...` hoặc thiếu thẻ đóng `</div>` gây lỗi render. | Thực hiện `write_file` toàn bộ nội dung file để đảm bảo cấu trúc JSX chính xác 100%. | ✅ Thành công |
| 5 | Không đồng bộ UI giữa các trang Admin | Mỗi trang dùng một bộ Icon (Phosphor vs Lucide) và bo góc khác nhau. | Đồng bộ toàn bộ về Lucide Icons, `brand-600` palette và `rounded-2xl` chuẩn Nexus Pro. | ✅ Thành công |
| 6 | SyntaxError: 'CursorPointer' not provided by 'lucide-react' | Sử dụng tên icon không tồn tại trong thư viện `lucide-react` mới. | Thay thế `CursorPointer` bằng `MousePointer2` trong `MarketingPage.jsx`. | ✅ Thành công |
| 7 | Lỗi lọc tài khoản theo Role (Admin/Expert) | Giá trị lọc ở Frontend viết thường (`admin`, `expert`) không khớp với dữ liệu viết hoa (`Admin`, `Expert`) trong MongoDB. | Cập nhật các giá trị tab lọc trong `AccountManagementPage.jsx` thành viết hoa chữ cái đầu. | ✅ Thành công |
| 8 | TypeError: Cannot read properties of undefined (reading 'length') | API Support Ticket trả về mảng trực tiếp, trong khi Frontend mong đợi một Object chứa field `data`. | Cập nhật Backend trả về `{ success: true, data: ... }` và thêm logic phòng thủ ở Frontend. | ✅ Thành công |
| 9 | ReferenceError: selectCurrentUser is not defined | Quên import selector Redux khi thêm logic kiểm tra role tại `LoginPage.jsx`. | Bổ sung `import { selectCurrentUser } ...` từ `authSlice.js`. | ✅ Thành công |
| 10 | PARSE_ERROR (Missing closing div) | Thêm logic hiển thị Badge Expert nhưng thiếu thẻ `</div>` đóng gây lỗi biên dịch Vite. | Cân bằng lại các thẻ HTML trong `Header.jsx`. | ✅ Thành công |
| 11 | Role Expert bị hạ cấp thành Customer | Backend `normalizeRole` chưa định nghĩa Expert nên tự động trả về Customer sau khi đăng nhập. | Cập nhật `authController.js` và `LoginPage.jsx` để giữ nguyên vai trò Expert và redirect đúng trang. | ✅ Thành công |
| 12 | ReferenceError: ReactDOM is not defined | Sử dụng `ReactDOM.createPortal` nhưng chỉ import `createPortal` từ `react-dom`. | Thay đổi `ReactDOM.createPortal` thành `createPortal` trong `ExpertDashboard.jsx`. | ✅ Thành công |
| 13 | PARSE_ERROR (Unexpected token </div>) | Do lỗi duplicate code và thẻ đóng dư thừa khi refactor drawer. | Dọn dẹp code dư thừa và cân bằng lại cấu trúc JSX cuối file `ExpertDashboard.jsx`. | ✅ Thành công |
| 14 | Lỗi hiển thị giá tiền (0đ) khi sửa chữa | Dữ liệu linh kiện (`usedParts`) trả về từ API chỉ có ID, thiếu thông tin giá (price) để tính toán. | Cập nhật Backend để `populate` trường `part` và ép kiểu `Number` khi tính `totalCost` ở Frontend. | ✅ Thành công |
| 15 | Màu sắc nút bấm không đồng bộ | Nút "Cập nhật tiến độ" dùng màu đen (`slate-900`) không khớp với theme xanh của dự án. | Chuyển toàn bộ nút chính và trạng thái active sang `brand-600` (Blue/Indigo). | ✅ Thành công |
